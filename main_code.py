# Create a class object that store the remaing value and the step we are in
class node:
  def __init__(self, value, step):
    self.value = value
    self.step = step
 
# Returns minimum number of operations
def min_operations(x, y):
  # A variable to keep track of visited numbers.
  visited = set()





print(min_operations(6, 20))
# (((6 - 1) * 2) * 2) = 20 : 3 operations needed only
#

